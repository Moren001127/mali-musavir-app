declare const process: {
  env: {
    EXPO_PUBLIC_API_URL?: string;
    /** 'tara' → yapı doğrudan Belge Tarayıcı ekranıyla açılır (evrak yükleme APK'sı). */
    EXPO_PUBLIC_MOREN_APP?: string;
  };
};

